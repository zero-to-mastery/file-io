#If you want to run directly -  use the following command : 

```javac SpotifyDataParser.java && java SpotifyDataParser``` 

Just make sure you are on the base directory hanintsolo-plain-script.

or more simply just do : 

``` java SpotifyDataParser.java ``` 

Note : make sure that you have compiled the SpotifyDataParser.java using javac